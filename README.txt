--- Clouds ---
A cross platform messenging app made using python and tkinter.


--- INSTALL ---
On Windows / Linux, installing dependencies can be done by running this in cmd / terminal:
    pip install pillow requests firebase-admin dropbox

On Linux, the dependencies can be loading by activating the virtual environment provided, which negates the need of installing the modules.

--- START ---
Just run clouds.py to start the app.

